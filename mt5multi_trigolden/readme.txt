如果要做單 需要取用哪幾個參數?
如果某個周期某個itv可以做單(選擇某週期的wider itv來做單) 那麼我需要輸出他的 商品名稱(第一列) code 基本週期 itv 空單還是多單(code)  FVG型態(code) 上 下界價錢(monitor) 在區間內的最高的綠色FVG價格 在區間內的最低的紅色FVG價格(tri.extreme) 目前價格(隨時可拿到)
如果是 XF 配上 2 4 的單子 則沒有最低紅色價格怎麼辦? 那麼紅色最低設成與上界的值相同即可, 如果是0xZZZZFFFF怎辦? 上界價錢是999999，這時可以參考最低紅色價格，那麼妳再問，如果沒有紅色FVG怎辦->則紅色FVG最低的價格會被設成999999 這是我的停利會被設成999999,這時候要有判斷式子，如果停利是999999，則停利點設成進場點*1.005的位子 相反的0000ZZZZ的單子也是這樣做

寫兩個EA 把處理資料的EA(以下稱為EAD)和交易的EA分開寫 交易的EA必須讀取處理資料的EA的輸出檔案，當交易的EA，以下稱為EAT，EAT把EAD的output file 讀取進去交易類，類裡面有矩陣稱為GOLDEN矩陣，每50秒讀取一次，每讀取一次都會確認進的單子是否已經再output file中消失，如果消失(找不到) 則把某個已經掛的週期某區間的單子給取消，取消的話可能需要處理單號，所以我要把處理單號也存進去另外的矩陣，叫已掛單矩陣，如果掛單矩陣找不到GOLDEN矩陣裡面的自己，代表已經消失 則取消掛單，已掛單矩陣裡面包含的元素必須要有處理單號，產品 週期 間隔，反正要跟GOLDEN有的差不多就對了，

成功進單之後利用處理單號去反向搜尋是哪個產品哪個週期哪個間隔，這時候就要用出場去找了 等到這一步之後先用人工出場.

0F找到單子之後要進一步確認BOS輸出的檔案的特定週期有沒有支撐或壓力
所以要做的是把0F的輸出和Monitor的輸出標準化比較好給trade ea跑